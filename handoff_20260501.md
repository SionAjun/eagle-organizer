# Eagle 打标项目 — 交接文档 v2.0(2026-05-01)

> 本文档替代 2026-04-30 的 handoff.md。继承所有未变更内容,在其基础上补本日进展。

---

## 项目状态速览

- 进度:**batch_023 / 380 张 / 共 20378 张**(无变化)
- 词表版本:tags.json **v2.2**(228 标签 / 15 前缀,无变化)
- 截止日期:**2026-05-28**(mimo Pro 套餐到期)
- mimo 余额:已用 ~3.5%(本日 v1+v2 共 10 张实弹消耗,可忽略)
- **本日新状态**:**mimo-v2.5 直连通路已打通,但风格未对齐(Jaccard 0.242),阻塞在「是否全切」的决策上**

---

## 三方协作架构(无变化)

- **此对话 Claude**(决策端,看图,出指令)
- **Claude Code**(工程端,本机执行,默认跑 mimo-Pro 后端省 token)
- **mimo-v2.5**(脚本直连,视觉打标主力,本日已接入)
- **用户**(中转 + 最终决策)

---

## 本日工作摘要(2026-04-30 ~ 2026-05-01)

### ① Step 1 关闭 — Jaccard 兼容性验证 ✅
- 5/5 张全等,平均 Jaccard = 1.000(远超 0.9 阈值)
- prompts 外置(Stage 2A)未引入任何打标漂移
- 产物:`reports/jaccard_test.md`、`reports/jaccard_test_results.json`

### ② Step 2A 完成 — mimo-v2.5 SDK 接入 + 版.txt 修复 ✅
**新增依赖**:openai 2.33.0 + python-dotenv 1.2.1(写入 requirements.txt)

**新增文件**:
- `.env` / `.env.example` / `.gitignore`(已加 .env)

**tag_real.py 改动**:
- 顶部 import:os / base64 / OpenAI / load_dotenv
- 常量区:MIMO_API_KEY / MIMO_BASE_URL / MIMO_MODEL / temperature=0.3 / top_p=0.95
- 新增 `build_messages(item)`:装配 system + user + 图片 base64
- 新增 `call_mimo(messages, max_retries=3)`:含 429/5xx 指数退避
- 新增 `_extract_tags(raw)`:容错 JSON 提取
- 新增 CLI `--test-llm <ITEM_ID>`:单张测试,不写状态

**版.txt 修复**:
- 病灶:`config/prompts/版.txt` 文件存在但 `load_prompt()` 从未调用,版- 判定硬编码在 `cmd_sync` 内联字符串
- 修复:删除硬编码,改用 `load_prompt("版.txt")` 注入 `{ban_txt}` 变量

**单张实弹通过**:MNIYMC2M2H0K9,12 秒,prompt=2286 / completion=1044 / cached=0(首次正常)

### ③ Step 2A 后置 — 5 张 mimo vs 旧打标对照 v1(发现严重问题)

| 指标 | v1 |
|---|---|
| 平均 Jaccard | **0.031** |
| 词表外标签率 | **70.8%**(17/24) |
| raw_response 违规 | 1 张内心独白 + 1 张 markdown 包裹 |

### ④ 根因诊断 — system 块词表丢失

**病灶定位**:`config/prompts/system.txt` 第 11 行

```
完整词表请直接读 tags.json,本指令不复述具体标签内容。
```

- CC 交互模式下,Claude 可 Read 本地文件,这句话有效
- **mimo API 调用下,模型无法访问本地文件**,228 标签中只有约 38 个被规则示例间接提到,其余 190 个对模型完全不可见
- 模型不是在选标签,是在凭空猜

**诊断报告**:`reports/prompt_debug.md`、`reports/_dump_system.txt`、`reports/_dump_user.txt`

### ⑤ Step 2A 补丁 — system 注入完整词表 + 输出格式硬约束 ✅

**tag_real.py 改动**:
- 新增 `render_tag_catalog()`:按前缀分组渲染 228 标签到 system 末段(吃 cache 最稳)
- `build_messages()` 拼接顺序:system.txt + 排异.txt + 格.txt + 版.txt + 自检.txt + **render_tag_catalog()**

**system.txt 改动**:
- 删除原误导句,改为「完整词表见本 system 末段『完整词表』章节,只允许从中选择标签」

**main.txt 改动**:
- 加强输出格式硬约束:首字符必须是 `{`、末字符必须是 `}`、不要思考过程、不要 markdown 包裹、违反丢弃

### ⑥ v2 复测 5 张 — 修复见效但风格仍未对齐

| 指标 | v1 | v2 | 变化 |
|---|---|---|---|
| 平均 Jaccard | 0.031 | **0.242** | +0.211(7.8 倍) |
| 词表外率 | 70.8% | **0%** | -70.8pp |
| 输出格式违规 | 1/5 | **0/5** | 清零 |
| 平均 prompt_tokens | 2,135 | 3,621 | +1,486(词表注入代价) |
| 平均 completion_tokens | 1,003 | 2,701 | +1,698(待优化) |
| 平均 cached_tokens | 1,280 | 1,638 | 第 2 张起稳定 2048 |
| 平均耗时 | 13.5s | 26.6s | +13s |

**Jaccard 逐张**:
| item | v1 | v2 |
|---|---|---|
| MDLQW3127UVMN | 0.000 | 0.125 |
| MNIYMC2M2H0K9 | 0.048 | 0.158 |
| MNHQPCPS0UGSM | N/A(空白图) | N/A |
| MNHNYAIV6EH06 | 0.000 | 0.353 |
| MNHNWSMHAPEBR | 0.077 | 0.333 |

---

## 人工抽检判定(此对话 Claude 看图)

> 用户已将 5 张原图截给此对话 Claude,以下为基于真图的判定。

### MDLQW3127UVMN — 写实3D机甲半身像
**画面**:写实3D渲染,胸前印 "HARBINGER",腿部 "UNIT 84",顶部彩色太阳能板,灰白军绿配色。

| 标签 | 旧打 | mimo v2 | 判定 |
|---|---|---|---|
| 类-3D | ✓ | | 旧对(应保留) |
| 类-概念图 | ✓ | ✓ | 都对 |
| 格-装甲核心6 | | ✓ | mimo 有价值的锚定 |
| 构-中心 | | ✓ | 对 |
| 角-半身 | | ✓ | 对 |
| 角-机器人 | ✓ | | 旧对(可与角-机甲共存) |
| 角-机甲 | | ✓ | 对 |
| 镜-全景 | ✓ | | 旧偏(实际为中景/近景) |
| 材-金属-光面 | ✓ | | 旧对(mimo 漏) |
| 色-低饱和 | ✓ | | 旧对(mimo 漏) |
| 题-军事 | ✓ | | 旧对 |
| 题-机甲 | ✓ | ✓ | 都对 |
| 题-硬科幻 | ✓ | | 旧对(mimo 漏) |
| 物-机甲 | | ✓ | 对 |
| **风-卡通** | | ✓ | **🔴 明显错误** |
| 风-写实3D | ✓ | | 旧对 |

**结论**:mimo 加打的格-装甲核心6 / 角-半身 / 物-机甲 都有价值,但 **风-卡通 是系统性误判**(写实3D 渲染被打成卡通),且漏打 材-/色-/风- 三个维度的关键标签。

### MNIYMC2M2H0K9 — 粉色尖顶建筑 + 防护服人
**画面**:白色尖顶建筑,粉色三角顶,蓝色拱形装饰,前景两个白色防护服角色。

| 标签 | 判定 |
|---|---|
| 场-工业区 (mimo) | 偏(应为建筑/广场场景) |
| 场-城市 (旧) | 可接受 |
| 角-平民 (mimo) | 可接受(穿防护服严格说不算平民) |
| 物-建筑 (mimo) | 对 |
| 材-混凝土 (mimo) | 对 |
| 色-冷暖对比 (mimo) | 对(粉+蓝+白) |
| 氛-宏大 (mimo) | 对 |
| 氛-神秘 (旧) | 旧更贴切 |
| 风-写实 (mimo) | 偏(实际为3D渲染,旧的 风-写实3D 更准) |

**结论**:维度互补,无致命错,可接受。

### MNHQPCPS0UGSM — 全黑+小红点
exception 已收录,双方均空,正确处理。

### MNHNYAIV6EH06 — 白塔 + 黄拱 + 蓝天
**画面**:仰视巨大白色塔楼,周围黄色拱形结构,天空有飞行物。

**双方一致**:类-概念图、镜-仰视、题-复古未来、氛-宏大、光-白天、风-写实3D

**mimo 加打的有价值标签**:材-混凝土、物-建筑、色-冷暖对比、构-中心(都对)

**结论**:这张是 v2 里 Jaccard 最高的(0.353),mimo 表现最好,加打的维度都是合理补充。

### MNHNWSMHAPEBR — 黑白机甲草图(全身)
**画面**:粗略手绘的灰阶机甲设计稿,单脚轮设计,全身。

| 标签 | 判定 |
|---|---|
| **类-角色原画** (mimo) | **🔴 明显错误**(应为机械设定/机甲设计图,不是角色) |
| 类-手绘 (旧) | 旧对(mimo 漏) |
| 类-概念图 (旧) | 旧对(mimo 漏) |
| 角-机甲 / 题-机甲 / 色-黑白 | 双方都对 |
| 题-硬科幻 / 镜-全景 / 风-半写实 (旧) | 旧三项对(mimo 都漏) |

**结论**:这张 mimo 表现最弱,3 个维度漏打、1 个明显错误。

### 整体人工抽检结论
- **明确错误 2 处**:风-卡通(用于写实图)、类-角色原画(用于机械草图)— 都是风格识别系统性偏差
- **维度漏打**:mimo 平均比旧打少 1-3 个维度(尤其色-、材-、风-)
- **mimo 优势**:格-锚点(主动识别游戏视觉来源)、材-/物- 维度更细
- **整体可用度**:**60-70%**,单张可用,批跑会持续累积小偏差

---

## 待决策项(下个对话开局必须先 钧 决定)

### 🔴 决策 1:是否接受 mimo 当前风格(P0,阻塞 Step 2B)

| 选项 | 含义 | 代价 | 风险 |
|---|---|---|---|
| A. **接受 mimo,直接推进** | Step 2B 接 batch 主流程,全切 mimo | 0 | mimo 系统性误判会铺到 19998 张 |
| B. **改用 Anthropic API** | 放弃 mimo,买 Anthropic key | 高(~$X / 1000 张) | 时间紧,5/28 截止 |
| C. **再迭代一轮 prompt** | 加 few-shot 示例 + 反例,纠风-卡通/类-角色原画类系统性误判 | 1-2 轮工时 | 不一定见效,但低成本 |

**此对话 Claude 倾向**:**C → A**(先一轮 few-shot 修系统性误判,如果纠正了风-卡通和类-角色原画两个明确错,就接受现状切 Step 2B)。

### 🟡 决策 2:派- 词表落地节奏(P1)

原 Step 4 计划是接入 mimo 后引入 派- 词表。现 mimo 已能稳定打 228 词表,可选:
- A. 现在落地 派- v1.0(23 标签),让 mimo 一开始就能用 派-
- B. 先 Step 2B 接 batch + 200 张试跑稳定后再加 派-(原计划)

**倾向**:B(降低同时变量数,先确认 mimo 在 228 词表上能稳跑)。

### 🟡 决策 3:残余 mimo 错误的修复策略(若决策 1 选 C)

需要钧确认是用规则(rules.json / prompt 文本)纠,还是用 few-shot 示例纠:
- 风-卡通误判:加 prompt「风-卡通仅适用于明显的卡通渲染/二维动画风,3D 真实感渲染不算」
- 类-角色原画误判:加 prompt「机械/载具/机甲设计图归 类-机械设定 或 类-概念图,不归 类-角色原画」

---

## 完整执行路线图(更新版)

```
✅ Step 1:Jaccard 实测(线 1 收尾)
✅ Step 2A:mimo SDK 接入 + 版.txt 修复
✅ Step 2A 补丁:词表注入 + 输出格式硬约束
🔴 决策点:接受 mimo 风格 / 改用 Anthropic / 再迭代 prompt   ← 现在卡这
⬜ Step 2B:接通 batch 主流程(限速、重试、cache 优化)
⬜ Step 3:5 张对照测试(若决策选 C 才需要)
⬜ Step 4:派- v1.0 词表落地(23 标签)
⬜ Step 5:200 张试跑 + 报告
⬜ Step 6:微调 → 全开余量(预计 5 小时跑完 19998 张)
```

---

## 派- v1.0 词表(锁定,共 23 标签,等 Step 4 落地)

> 原文档保留,本日未改动。完整词表见 v1 handoff 文档。

简列:
- 现代主义系(4):瑞士国际主义/包豪斯/极简主义/风格派
- 后现代/反叛系(4):Brutalism/解构主义/后现代主义/孟菲斯
- 未来想象系(8):Atompunk/Dieselpunk/Solarpunk/赛博朋克美学/Vaporwave/赛博颓废/高科技低生活/太空时代
- 时代怀旧系(5):Y2K/Frutiger Aero/Lo-Fi/Art Deco/世纪中期现代
- 实验/数字系(2):故障艺术/像素美学

---

## mimo-v2.5 关键参数(无变化)

| 项 | 值 |
|---|---|
| RPM | 100(用 80) |
| TPM | 10M |
| Credit 比例 | 1:1 |
| 缓存命中价 | $0.08/1M |
| 未命中输入 | $0.40/1M |
| 输出 | $2.00/1M |
| 0:00-8:00 北京时间 | 0.8x 折扣 |
| temperature / top_p | 0.3 / 0.95(已生效) |
| base_url | https://token-plan-cn.xiaomimimo.com/v1 |
| model | mimo-v2.5(不是 pro,pro 无视觉) |

---

## 文件路径速查(供下个对话 Claude 用)

**项目根**:`C:\GAME 2333\game\Claud cod\eagle-organizer`

**关键文件**:
- 主脚本:`tag_real.py`(已含 build_messages / call_mimo / --test-llm)
- 配置:`config/prompts/`(system / main / 排异 / 格 / 版 / 自检)
- 词表:`config/tags.json` v2.2
- 规则:`config/rules.json`
- 异常:`config/exceptions.json`
- 环境变量:`.env`(MIMO_API_KEY 已填,不要打印值)

**本日产出报告**:
- `reports/jaccard_test.md`(线 1 验收)
- `reports/llm_call_snapshot.md`(LLM 调用现状探查)
- `reports/mimo_vs_old.md`(v1 对照)
- `reports/prompt_debug.md`(根因诊断)
- `reports/_dump_system.txt` / `_dump_user.txt`(完整 prompt 落盘)
- `reports/mimo_vs_old_v2.md`(v2 对照,关键)
- `reports/v2_human_review.md`(人工抽检模板)
- `reports/test_llm_*.json`(5 张 v2 单张产物)

---

## 已知遗留问题(优先级低,继承自 v1.3)

- `--validate` 命令未实现
- records 与 processed_ids 不一致(沉没成本)
- v2.2 词表 0 张实战(将随 mimo 接入首次实战)
- 前 380 张老资产打标风格 vs 未来 mimo 风格会断裂(可接受)

---

## 新对话 Claude 开局指引

1. 读完本文档(全文即上下文)
2. 先问钧关于「决策 1」的选择(A/B/C)
3. 按决策结果出对应指令包:
   - 决策 1=A → 出 Step 2B 改造指令(改 cmd_apply_batch / cmd_prepare,加限速、429 重试、cache 优化)
   - 决策 1=B → 出 Anthropic SDK 接入指令(类似 Step 2A 但换 client)
   - 决策 1=C → 出 few-shot 注入指令(收 5-10 张人工标准答案 → 加入 system prompt → 再跑 5 张对照)
4. 不需要钧再发 config 文件,Claude Code 那边的 /config/ 是真源,需要时让用户导出对应文件即可

---

## 此对话 Claude 给下个 Claude 的备注

- mimo 直连通路完全打通,系统层面没问题,剩下都是「prompt 调优」工作
- Jaccard 不是好的判断指标(继续追 0.7+ 没意义),改用人工抽检 + 错误率
- 主要精力放在 system block 的稳定性(吃 cache 关键)
- 建议把 ban_txt / 自检.txt / 排异.txt / 格.txt / 版.txt / 词表 都视作稳定前缀,只让 main.txt 的动态字段(total)留在 user 块
- 钧目前用 mimo-Pro 跑 CC,不要让 CC 看图(Pro 无视觉),所有看图任务交给此对话 Claude
