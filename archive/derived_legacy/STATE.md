# Eagle 素材库打标签项目 — 当前状态

最后更新：2026-04-26 01:35　　进度：220 / 20378 张

---

## 当前进度

- **已处理**：220 / 20378 张（1.08%）
- **最后处理**：`MOD0BX1EKR8PD`　`—`
- **待审建议标签**：['氛-动感', '风-平涂', '氛-复古', '类-场景设定', '类-角色设定', '构-俯视', '格-Starfield', '题-末世']

---

## 已完成事项

- Eagle API 连通性验证（`http://localhost:41595/api`，Eagle 4.0 Build 20250627）
- 资源库：素材，路径 `D:\杂项\素材.library`，共 20378 张 / 121 个文件夹
- dry_run.py 骨架验证通过（分页、排序、断点续跑全部正常）
- tags.json 词表生成完毕（version 2.1，227 个标签，15 个前缀）
- tag_real.py 真实打标脚本验证通过，链路完整（取图→看图→打标→写回→落盘）
- 检查点机制已建立（每 50 张自动触发，含 REPORT.md 覆盖写入）
- 批量读图模式（每批 20 张，tags.json 一次加载）

---

## 踩过的坑与解决方案

| 坑 | 现象 | 解决方案 |
|---|---|---|
| 系统代理劫持 localhost | curl/urllib 走 127.0.0.1:7892，返回 502 | `urllib.request.ProxyHandler({})` 绕过 |
| limit 小时分页静默截断 | `limit=50` 只能访问约 440 条 | 统一用 `limit=1000` |
| Eagle API 无总数字段 | `/api/item/list` 无 total | `limit=25000` 探底得 20378 |
| orderBy=-CREATEDATE 排序反了 | 加参数后返回 1970 epoch 老素材 | 去掉 orderBy，依赖 Eagle 默认倒序 |
| btime 字段大量为 0 | 老素材 btime=Unix 纪元 | 改用 `modificationTime` 字段 |
| `/api/v2/` 端点不存在 | Eagle 4.0 没有 v2 API | 全部使用 `/api/` 前缀 |
| limit=200 offset=200 返回空 | Eagle API limit 影响可访问总量 | 用大 limit（1000）单次拉取 |

---

## 下一步待办

1. 执行 `python tag_real.py --prepare --limit 20`
2. 一次读取全部 20 张图，输出 JSON 写入 batch_results.json
3. 执行 `python tag_real.py --apply-batch`
4. 每 50 张自动触发检查点（含 REPORT.md 生成）
5. 累积建议标签后，与用户确认是否加入词表并更新 version

---

## 关键约束（不可违反）

- 标签严格从 `tags.json` 词表选，**绝不自由发挥**
- 词表版本 2.1，共 227 个标签，15 个前缀：类、题、风、格、版、氛、光、镜、构、场、角、物、材、色、教
- 每张图只做一次视觉分析
- 新素材保护期：btime < 24 小时的跳过
- 分页：`limit=1000`，不传 `orderBy`
- 写回前先拉旧标签合并，不覆盖用户手动标签
- 词表外概念写 `suggested_tags.json`，不擅自加到素材
- 批量模式：一次 20 张，tags.json 只加载一次

---

## 给下次会话 Claude Code 的指令（Eagle 素材打标 v2.1，2026-04-26 01:35）

### 你的角色
你是本项目的打标执行端。你看图、从 tags.json 选标签、输出 JSON。
不自由发挥，不擅自创建词表外标签。

### 词表
当前 tags.json 版本：2.1
标签总数：227
前缀总数：15
前缀列表：类、题、风、格、版、氛、光、镜、构、场、角、物、材、色、教

完整词表请直接读 tags.json，本指令不复述具体标签内容。

### 启动顺序
1. 读完本文件（STATE.md）
2. 读 `tags.json`（完整词表，227 个标签）
3. 读 `progress.json`（已处理 ID，打标时跳过）
4. 读 `suggested_tags.json`（待审建议标签，先展示给用户）
5. 按用户指示继续执行批量流程（prepare → 读图输出 JSON → apply-batch）

### 标签选择优先级

**格-（最高权重，主检索入口）**
用户是游戏美术/概念设计师，翻图主要是为了"偷美学"。
只要图片视觉风格明确对应某个游戏/作品体系，必须补对应 格- 标签。
有 风- 标签不等于不需要 格-，两者并存是常态。
拿不准时宁可漏补，不要错配。

**版-（次高权重）**
类-UI / 类-排版 图片：必须判定 版- 维度。
其他图片：视情况判定。

**排异规则**
- 类-UI / 类-排版 / 类-实景参考：屏蔽 光/镜/氛/场 维度
- 类-UI / 类-排版：放行 格/版

### 格- 候选缩窄规则

R1:  含 题-赛博朋克 → [格-赛博朋克2077, 格-命运2, 格-控制]
R2:  含 题-都市幻想 或 题-近未来 → [格-绝区零, 格-控制, 格-VA-11 HALL-A]
R3:  含 题-剑与魔法 + (角-立绘 或 类-角色原画) → [格-原神, 格-塞尔达王国之泪, 格-魂系]
R4:  含 题-科幻 + (物-机甲 或 类-机械设定) → [格-装甲核心6, 格-质量效应, 格-异形:隔离, 格-命运2]
R5:  含 题-末世 / 题-废土 / 题-后启示录 → [格-辐射系列, 格-逃离塔科夫, 格-死亡搁浅]
R6:  含 题-恐怖 或 氛-诡异 → [格-异形:隔离, 格-控制, 格-雨世界]
R7:  含 风-像素 或 风-2D横版 → [格-Hades, 格-雨世界, 格-VA-11 HALL-A]
R8:  含 题-奇幻 且不含 R3 条件 → [格-原神, 格-塞尔达王国之泪, 格-魂系, 格-Hades]
R9:  含 题-复古未来 → [格-辐射系列, 格-极乐迪斯科, 格-VA-11 HALL-A]
R10: 含 风-日系 → [格-明日方舟, 格-原神, 格-绝区零]

S1:  已含任意 格- → skip 格-（不再补）
S2:  含 类-实景参考 → skip 格-
S3:  含 类-照片 / 类-摄影 → skip 格-
S4:  含 类-游戏截图 → skip 格-

未命中任何规则 → 自由从全部 格- 中选，拿不准则不补。

### 强制自检规则（每张图打标后必走）
打标输出前，对 tags_to_add 里每一个标签执行检查：
1. 该标签是否完整存在于 tags.json？（前缀 + 内容都要对）
2. 不存在 → 立刻从 tags_to_add 移除
3. 不存在但概念有价值 → 追加到 suggested 字段
4. 不存在且概念无价值 → 丢弃

**错误示范（绝对禁止）：**
- tags.json 有"构-"前缀但无"构-俯视" → 不能写"构-俯视"
- tags.json 有"题-废土""题-后启示录"但无"题-末世" → 不能写"题-末世"
- tags.json 有"类-角色原画"但无"类-角色设定" → 不能写"类-角色设定"
- tags.json 有"类-概念图"但无"类-场景设定" → 不能写"类-场景设定"

**正确做法：**
- 看到俯视构图 → 选 tags.json 里已有的近义构标签，无则不补构
- 看到末世题材 → 写 题-废土 或 题-后启示录（已有），不写 题-末世
- 看到角色设定图 → 写 类-角色原画，不写 类-角色设定

### 词表外概念
看图发现词表无法表达的概念 → 写入 suggested 字段，不写入 tags_to_add。
suggested 累计 count ≥ 3 由用户决定是否升入词表。

### 输出格式
batch_results.json 内每条：
{"item_id": "xxx", "tags_to_add": ["前缀-内容", ...], "suggested": ["新概念", ...]}

无法归类的图：tags_to_add 留空 []，并在回复正文标注：
挂起：item_id（原因）

### 安全约束
- 一张图只做一次视觉分析，不重复消耗 token
- 写回前脚本会自动合并旧标签，不覆盖手动标签
- 每 50 张自动触发检查点

**当前进度：已处理 220 张 / 20378**
